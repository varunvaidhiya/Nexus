# packages/schema

Canonical normalized types (Conversation, Message, ...) shared by every
ingestion tier and the backend — JSON Schema as source of truth, with generated
Pydantic models (Python) and TypeScript types. This is the contract for `POST /ingest`.

Defined in **Phase 0** (see [docs/IMPLEMENTATION_PLAN.md](../../docs/IMPLEMENTATION_PLAN.md)).
