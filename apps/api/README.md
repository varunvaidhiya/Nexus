# apps/api

FastAPI backend — ingest normalizer, context engine (embeddings, summaries,
rolling profile), provider gateway, assistant engine, handoff service, MCP server.

Scaffolded in **Phase 0** (see [docs/IMPLEMENTATION_PLAN.md](../../docs/IMPLEMENTATION_PLAN.md)).
